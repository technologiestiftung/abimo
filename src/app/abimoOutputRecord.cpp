#include "abimoOutputRecord.h"

AbimoOutputRecord::AbimoOutputRecord() :
    code_CODE(QString("")),
    totalRunoff_R(0.0),
    runoff_ROW(0.0),
    infiltrationRate_RI(0.0),
    totalRunoffFlow_RVOL(0.0),
    rainwaterRunoff_ROWVOL(0.0),
    totalSubsurfaceFlow_RIVOL(0.0),
    totalArea_FLAECHE(0.0),
    evaporation_VERDUNSTUN(0.0)
{
}
